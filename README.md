
---

## 🔧 Requirements

Make sure the following packages are installed:

| Tool | Purpose | Install command (Ubuntu/Debian) |
|------|----------|----------------------------------|
| Flex | Lexer generator (tokenizer) | `sudo apt-get install flex` |
| Bison | Parser generator (grammar) | `sudo apt-get install bison` |
| GCC | C compiler | `sudo apt-get install build-essential` |

---

## 📦 Project Structure

